



    const foldedCorner = document.getElementById('folded-corner');

    foldedCorner.addEventListener('click', () => {
        window.scrollTo({
            top: 0,
            behavior: 'smooth'
        });
    });
 
